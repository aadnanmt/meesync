{
  "imports": {},
  "tasks": {
    "start": "deno run -A --env-file scripts/index.ts",
    "run": "deno run -A --env-file scripts/index.ts ./README.md ./stats.json",
    "format": "deno fmt",
    "fmt:check": "deno fmt --check",
    "setup": "cp .env.example .env || copy .env.example .env"
  },
  "fmt": {
    "semiColons": false,
    "lineWidth": 80,
    "indentWidth": 2,
    "singleQuote": true
  }
}
